//! Local vendored BDD helpers for pheno-plugin-vessel tests.
//!
//! Kept intentionally minimal because this crate does not currently ship a full
//! external cucumber step dependency set.

/// Placeholder hook for legacy BDD entry points that still reference this helper
/// module path.
pub fn initialize_bdd_shim() {}
